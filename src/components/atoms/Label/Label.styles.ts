import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  label: {
    fontFamily: 'Poppins',
    fontSize: 16,
    lineHeight: 24,
    color: '#FFFFFF',
    fontWeight: '600',
  },
});
