import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  networkListItem: {},
  contentWrapper: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  dot: {
    width: 12,
    height: 12,
    borderRadius: 12,
    marginRight: 18,
  },
});
